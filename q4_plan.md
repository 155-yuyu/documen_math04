# 问题四：多区域算—储—电协同优化模型 — 建模方案

## 1 问题分析

### 1.1 问题描述

在前三问基础上，建立多区域算—储—电协同优化模型，统一考虑任务迁移、电价、碳排放、可用新能源、储能、区域购售电边界、网络时延及任务异构性。在系统运行成本、碳排放、网络时延、服务质量、新能源利用率和区域峰值净购电之间进行权衡，并比较不同碳约束、电价机制和新能源波动场景下的策略变化。

### 1.2 问题定位

| 维度 | 说明 |
|------|------|
| 问题类型 | 大规模混合整数非线性优化（MINLP）→ 分层分解为启发式+LP |
| 优化对象 | 任务迁移+开工时段 **联合** 储能充放电+购售电+新能源分配 |
| 输入 | 50,000任务负载、逐时电力参数（电价/售电价/碳强度/新能源）、储能参数、GPU容量、网络时延 |
| 决策变量 | 任务级（离散）+ 储能级（连续），跨层级耦合 |
| 目标 | 6个目标：成本、碳排、时延、QoS、新能源利用率、峰值净购电 |
| 时间域 | 0–2406小时（主时域0–2399，收尾2400–2405，终端结算2406） |
| 特殊要求 | 场景对比分析：不同碳约束、电价机制、新能源波动 |

### 1.3 与前三问的关系

| 对比维度 | 问题二 | 问题三 | 问题四 |
|----------|--------|--------|--------|
| 优化对象 | 任务迁移+开工时段 | 储能充放电+购售电 | **任务+储能联合优化** |
| IT负荷 | 由调度计算 | 给定（Baseline） | **由调度计算（动态）** |
| 储能策略 | 固定基准 | 优化对象 | **联合优化对象** |
| 目标数 | 4个 | 4个 | **6个** |
| 问题性质 | 大规模组合优化 | 线性规划 | **混合整数非线性** |
| 场景分析 | 无 | 无 | **3类场景对比** |

### 1.4 问题特点

1. **跨层级耦合**：任务调度决定IT负荷→IT负荷决定设施负荷→设施负荷影响储能优化→储能优化影响成本/碳排→成本/碳排反馈影响任务调度偏好
2. **多目标冲突**：6个目标之间存在复杂权衡关系（如低成本vs低时延，低碳排vs高QoS）
3. **大规模**：50,000任务×6区域 + 6区域×2407小时×7变量 ≈ 30万+决策变量
4. **异构性**：三类任务的时延敏感度、功率特性、调度弹性差异极大
5. **场景多样性**：需在碳约束、电价机制、新能源波动3个维度上进行场景对比

---

## 2 数据集分析

### 2.1 任务负载数据（workload_trace.xlsx）

| 统计项 | AITraining | BatchInference | RealTimeInference |
|--------|-----------|---------------|-------------------|
| 任务数 | 16,559 | 16,717 | 16,724 |
| GPU_Demand均值 | 64.5 | 16.5 | 5.6 |
| 时长均值(min) | 226.0 | 166.4 | 120.0 |
| MaxLatency(ms) | 150 | 80 | 20 |
| GPU-hours总量 | 4,023,539 | 769,098 | 228,150 |
| 调度弹性 | 完全灵活 | 中等灵活 | 完全固定 |

**关键观察**：
- AITraining贡献80.2%的GPU-hours，是算力调度的核心优化对象
- RealTimeInference时延仅20ms，几乎不能迁移（仅限源区域+相邻区域）
- BatchInference时延80ms，可迁移至部分西部区域
- AITraining时延150ms，可迁移至全部6个区域

### 2.2 区域参数

**表1 GPU容量与电力参数**

| 区域 | 可用GPU | 最大IT功率(MW) | PUE | 最大设施功率(MW) | 均值电价(元/MWh) | 均值碳强度 | 新能源均值(MW) |
|------|--------|--------------|-----|----------------|----------------|----------|-------------|
| RegionA | 630 | 420 | 1.35 | 567.0 | 708.1 | 0.620 | 800.0 |
| RegionB | 585 | 410 | 1.35 | 553.5 | 678.6 | 0.580 | 800.0 |
| RegionC | 540 | 405 | 1.38 | 558.9 | 659.0 | 0.550 | 800.0 |
| RegionD | 1,472 | 720 | 1.28 | 921.6 | 422.9 | 0.420 | 800.0 |
| RegionE | 1,012 | 560 | 1.25 | 700.0 | 373.7 | 0.220 | 800.0 |
| RegionF | 966 | 540 | 1.27 | 685.8 | 393.4 | 0.260 | 800.0 |

**表2 储能参数**

| 区域 | 容量(MWh) | 初始SOC(MWh) | 最大充(MW) | 最大放(MW) | η_ch | η_dis | 最大购电(MW) | 最大售电(MW) |
|------|----------|-------------|-----------|-----------|------|-------|------------|------------|
| A | 350 | 157.5 | 100 | 100 | 0.93 | 0.92 | 550 | 0 |
| B | 320 | 144.0 | 90 | 90 | 0.93 | 0.92 | 520 | 0 |
| C | 300 | 135.0 | 85 | 85 | 0.93 | 0.92 | 510 | 0 |
| D | 900 | 405.0 | 260 | 260 | 0.94 | 0.93 | 510 | 180 |
| E | 820 | 370.0 | 240 | 240 | 0.94 | 0.93 | 370 | 220 |
| F | 850 | 382.5 | 250 | 250 | 0.94 | 0.93 | 340 | 220 |

**表3 网络时延矩阵（ms）**

| 源\目标 | A | B | C | D | E | F |
|---------|---|---|---|---|---|---|
| A | 5 | 12 | 15 | 65 | 78 | 82 |
| B | 12 | 5 | 14 | 60 | 74 | 79 |
| C | 15 | 14 | 5 | 58 | 70 | 76 |
| D | 65 | 60 | 58 | 5 | 22 | 25 |
| E | 78 | 74 | 70 | 22 | 5 | 18 |
| F | 82 | 79 | 76 | 25 | 18 | 5 |

### 2.3 关键数据特征

1. **新能源充裕**：各区域新能源均值800 MW，设施负荷均值约450 MW，新能源供给充足
2. **东西部差异显著**：东部A/B/C电价高（659–708元/MWh）、碳强度高（0.55–0.62）、无售电能力；西部D/E/F电价低（374–423元/MWh）、碳强度低（0.22–0.42）、可售电
3. **时延约束分层**：RT(20ms)→仅源区域；Batch(80ms)→源+邻区；Training(150ms)→全区域
4. **储能能力分层**：东部小容量(300-350MWh)无售电；西部大容量(820-900MWh)可售电

---

## 3 数学模型

### 3.1 集合与索引

| 符号 | 含义 |
|------|------|
| $\mathcal{R} = \{A, B, C, D, E, F\}$ | 区域集合 |
| $\mathcal{I}$ | 任务集合，$|\mathcal{I}| = 50{,}000$ |
| $\mathcal{I}^{RT}, \mathcal{I}^{Batch}, \mathcal{I}^{Train}$ | 任务子集 |
| $\mathcal{T} = \{0, 1, \ldots, 2406\}$ | 时间步集合 |
| $\mathcal{T}_{\text{main}} = \{0, \ldots, 2399\}$ | 主运行时域 |

### 3.2 决策变量

#### 3.2.1 任务级决策变量（离散）

| 变量 | 类型 | 含义 |
|------|------|------|
| $x_{i,r}$ | 二元 | 任务$i$是否分配到区域$r$执行 |
| $s_i$ | 整数 | 任务$i$的开工小时 |

#### 3.2.2 储能级决策变量（连续，每区域每小时）

| 变量 | 含义 | 单位 |
|------|------|------|
| $p^{\text{ren,dir}}_{r,t}$ | 新能源直接供电 | MW |
| $p^{\text{ren,ch}}_{r,t}$ | 新能源充电 | MW |
| $p^{\text{ren,sell}}_{r,t}$ | 新能源售电 | MW |
| $p^{\text{curt}}_{r,t}$ | 弃风弃光 | MW |
| $p^{\text{grid,ch}}_{r,t}$ | 电网充电 | MW |
| $p^{\text{grid,load}}_{r,t}$ | 电网供电 | MW |
| $p^{\text{dis,load}}_{r,t}$ | 放电供负荷 | MW |
| $p^{\text{dis,sell}}_{r,t}$ | 放电售电 | MW |

#### 3.2.3 耦合变量

| 变量 | 计算式 |
|------|--------|
| AI IT负荷 $L^{\text{AI}}_{r,t}$ | $\sum_{i \in \mathcal{I}} x_{i,r} \cdot g_i \cdot \text{Overlap}(i, r, t) \cdot p^{\text{gpu}}_i$ |
| 总IT负荷 $L^{\text{IT}}_{r,t}$ | $L^{\text{AI}}_{r,t} + L^{\text{NonAI}}_{r,t}$ |
| 设施负荷 $L^{\text{fac}}_{r,t}$ | $L^{\text{IT}}_{r,t} \times \text{PUE}_r$ |

其中 $\text{Overlap}(i, r, t)$ 表示任务$i$在区域$r$第$t$小时是否正在运行（由$s_i$和执行时长决定）。

### 3.3 目标函数

#### F1. 系统运行成本最小化

$$\min F_1 = \sum_{r \in \mathcal{R}} \sum_{t \in \mathcal{T}} \left[ P^{\text{buy}}_{r,t} \cdot \pi_{r,t} - P^{\text{sell}}_{r,t} \cdot \sigma_{r,t} \right]$$

#### F2. 碳排放最小化

$$\min F_2 = \sum_{r \in \mathcal{R}} \sum_{t \in \mathcal{T}} P^{\text{buy}}_{r,t} \cdot e_{r,t}$$

#### F3. 加权网络时延最小化

$$\min F_3 = \sum_{i \in \mathcal{I}} \sum_{r \in \mathcal{R}} x_{i,r} \cdot g_i \cdot \frac{d_i}{60} \cdot \text{Lat}(src_i, r)$$

其中以GPU-hours为权重，反映时延对算力的影响。

#### F4. 服务质量最大化

以任务按时完成率衡量：

$$\max F_4 = \frac{|\{i : s_i + \lceil d_i / 60 \rceil \leq f_i\}|}{|\mathcal{I}|}$$

等价地，最小化超时任务数：

$$\min F_4' = |\{i : s_i + \lceil d_i / 60 \rceil > f_i\}|$$

#### F5. 新能源利用率最大化

$$\max F_5 = \frac{\sum_{r,t} \left( p^{\text{ren,dir}}_{r,t} + p^{\text{ren,ch}}_{r,t} + p^{\text{ren,sell}}_{r,t} \right)}{\sum_{r,t} R_{r,t}}$$

#### F6. 区域峰值净购电最小化

$$\min F_6 = \max_{t \in \mathcal{T}} \sum_{r \in \mathcal{R}} P^{\text{net}}_{r,t}$$

#### 多目标综合

$$\min F = \sum_{k=1}^{6} w_k \cdot \tilde{F}_k$$

其中 $\tilde{F}_k$ 为归一化后的第 $k$ 个目标，$w_k$ 为权重。

### 3.4 约束条件

#### 3.4.1 任务调度约束

**T1. 区域唯一性**：$\sum_{r} x_{i,r} = 1, \quad \forall i$

**T2. 时延约束**：$x_{i,r} \cdot \text{Lat}(src_i, r) \leq L^{\text{max}}_i, \quad \forall i, r$

**T3. 开工时段约束**：$a_i \leq s_i \leq f_i - \lceil d_i / 60 \rceil, \quad \forall i$

**T4. 完成时限约束**：$s_i + \lceil d_i / 60 \rceil \leq f_i, \quad \forall i$

**T5. RT任务固定**：$s_i = a_i, \quad \forall i \in \mathcal{I}^{RT}$

**T6. GPU容量约束**：$\sum_{i} x_{i,r} \cdot g_i \cdot \text{Overlap}(i, r, t) \leq G^{\text{avail}}_r, \quad \forall r, t$

**T7. IT功率约束**：$L^{\text{AI}}_{r,t} \leq P^{\text{IT,max}}_r, \quad \forall r, t$

**T8. 设施功率约束**：$L^{\text{fac}}_{r,t} \leq P^{\text{fac,max}}_r, \quad \forall r, t$

#### 3.4.2 储能约束（同问题三）

**S1. 新能源平衡**：$p^{\text{ren,dir}} + p^{\text{ren,ch}} + p^{\text{ren,sell}} + p^{\text{curt}} = R_{r,t}$

**S2. 负荷供电平衡**：$p^{\text{ren,dir}} + p^{\text{grid,load}} + p^{\text{dis,load}} = L^{\text{fac}}_{r,t}$

**S3. SOC动态方程**：$E_{r,t} = E_{r,t-1} + \eta^{\text{ch}}_r P^{\text{ch}}_{r,t} - P^{\text{dis}}_{r,t}/\eta^{\text{dis}}_r$

**S4. SOC上下限**：$\underline{E}_r \leq E_{r,t} \leq \overline{E}_r$

**S5. SOC终端约束**：$E_{r, t_{\text{end}}} \geq E^{\text{init}}_r$

**S6–S10.** 充放电功率、购售电功率、新能源售电限制、非负约束（同问题三）

#### 3.4.3 耦合约束

**C1. 设施负荷一致性**：$L^{\text{fac}}_{r,t}$ 由任务调度结果计算，同时作为储能约束S2的输入

---

## 4 求解策略

### 4.1 问题性质分析

| 判据 | 结论 |
|------|------|
| 变量类型 | 混合（二元+整数+连续） |
| 目标函数 | 非线性（含max函数、绝对值、计数函数） |
| 约束类型 | 非线性（Overlap依赖$s_i$和$x_{i,r}$） |
| 问题规模 | ~30万变量（含50,000×6二元 + 50,000整数 + ~11.5万连续） |
| 可解性 | 精确MINLP不可行，需分层分解 |

### 4.2 分层分解策略

采用**"上层任务调度 + 下层储能优化"**的两层分解框架：

```
┌─────────────────────────────────────────────┐
│  上层：碳感知任务调度（启发式）              │
│  输入：任务负载、电力参数、时延矩阵           │
│  决策：x_{i,r}, s_i                          │
│  目标：成本+碳排+时延+QoS（加权综合）         │
│  输出：各区域逐时IT负荷 L^{AI}_{r,t}         │
└──────────────────┬──────────────────────────┘
                   ↓ L^{fac}_{r,t}
┌─────────────────────────────────────────────┐
│  下层：储能协同优化（LP）                     │
│  输入：设施负荷、电力参数、储能参数           │
│  决策：储能充放电+购售电+新能源分配           │
│  目标：成本+碳排+峰值+波动+新能源利用率       │
│  输出：储能策略、最终系统指标                 │
└─────────────────────────────────────────────┘
```

### 4.3 上层：碳感知任务调度

**任务分流**：
1. RealTimeInference：固定源区域，到达即开工（不可迁移）
2. BatchInference：在满足80ms时延约束的候选区域内优化
3. AITraining：在全部6区域内优化（150ms时延约束无约束力）

**加权综合评分**：

对每个可调度任务$i$，在候选区域$r$和时段$s$的放置评分为：

$$\text{Score}(i, r, s) = w_1 \cdot \tilde{C}^{\text{energy}}(i, r, s) + w_2 \cdot \tilde{C}^{\text{carbon}}(i, r, s) + w_3 \cdot \tilde{C}^{\text{latency}}(i, r) + w_4 \cdot \tilde{C}^{\text{renewable}}(i, r, s)$$

其中：
- $\tilde{C}^{\text{energy}}$：归一化能源成本（考虑电价和PUE差异）
- $\tilde{C}^{\text{carbon}}$：归一化碳排放（考虑碳强度差异）
- $\tilde{C}^{\text{latency}}$：归一化时延
- $\tilde{C}^{\text{renewable}}$：归一化新能源匹配度（任务时段新能源高出力→高匹配度）

**储能感知增强**：在评分中引入储能机会因子，当目标区域当前时段新能源富余且储能可充电时，给予额外奖励。

### 4.4 下层：储能协同优化

给定上层输出的IT负荷，下层问题退化为问题三的LP形式，采用HiGHS精确求解。

### 4.5 Pareto权重扫描

设置多组权重进行Pareto前沿扫描：

| 策略 | $(w_1, w_2, w_3, w_4)$ | 特点 |
|------|------------------------|------|
| 成本优先 | (0.70, 0.10, 0.10, 0.10) | 最大化电价套利 |
| 碳排优先 | (0.10, 0.70, 0.10, 0.10) | 最大化低碳区域消纳 |
| 时延优先 | (0.10, 0.10, 0.70, 0.10) | 最低时延 |
| 新能源优先 | (0.10, 0.10, 0.10, 0.70) | 最大化新能源匹配 |
| 均衡策略 | (0.25, 0.25, 0.25, 0.25) | 四维均衡 |
| 成本-碳排均衡 | (0.40, 0.30, 0.15, 0.15) | 兼顾经济与环境 |

### 4.6 场景分析设计

#### 4.6.1 碳约束场景

| 场景 | 碳约束 | 说明 |
|------|--------|------|
| 无碳约束 | $F_2$ 无上限 | 纯经济优化 |
| 中等碳约束 | $F_2 \leq$ 基准×50% | 碳排减半 |
| 严格碳约束 | $F_2 \leq$ 基准×10% | 近零碳排 |
| 零碳约束 | $F_2 = 0$ | 完全零碳 |

#### 4.6.2 电价机制场景

| 场景 | 电价机制 | 说明 |
|------|---------|------|
| 基准TOU | 当前分时电价 | 谷/平/峰三段 |
| 平价机制 | 各区域统一均价 | 消除时段套利空间 |
| 极端峰谷 | 峰谷价比扩大2倍 | 强化套利激励 |

#### 4.6.3 新能源波动场景

| 场景 | 新能源出力 | 说明 |
|------|-----------|------|
| 基准 | 原始数据 | 800 MW均值 |
| 高新能源 | ×1.20 | 新能源充裕 |
| 低新能源 | ×0.80 | 新能源不足 |

---

## 5 程序结构设计

```
03_模型程序/
├── data_loader.py          # 统一数据加载（任务+电力+储能+GPU+时延）
├── task_scheduler.py       # 上层：碳感知任务调度（启发式）
├── storage_optimizer.py    # 下层：储能协同优化（LP/HiGHS）
├── metrics.py              # 6维指标计算
├── scenario_analysis.py    # 场景分析（碳约束/电价/新能源）
├── visualization.py        # 可视化（12+张图）
└── main.py                 # 主程序（串联全流程）
```

### 5.1 各模块功能

| 模块 | 功能 |
|------|------|
| `data_loader.py` | 加载全部数据文件，构建numpy数组，计算基准指标 |
| `task_scheduler.py` | 任务分流→加权评分→贪心放置→输出IT负荷 |
| `storage_optimizer.py` | 构建稀疏LP→HiGHS求解→提取储能策略 |
| `metrics.py` | 计算6维指标（成本/碳排/时延/QoS/利用率/峰值） |
| `scenario_analysis.py` | 3类场景×多参数对比分析 |
| `visualization.py` | 调度甘特图、SOC轨迹、对比柱状图、Pareto前沿、场景对比图 |
| `main.py` | 基准→优化→Pareto→场景分析→保存结果 |

---

## 6 时域处理

| 时段 | 小时范围 | 处理方式 |
|------|---------|---------|
| 主运行时域 | 0–2399 | 完整优化，任务到达+调度+储能 |
| 收尾时域 | 2400–2405 | 结清末端任务，参与能源结算 |
| 终端结算 | 2406 | 仅SOC终端状态结算 |

---

## 7 预期成果

1. **基准对比**：与问题二/三基准策略逐项对比6维指标
2. **Pareto前沿**：6组权重策略的多目标权衡前沿
3. **场景分析**：3类场景（碳约束/电价/新能源）×多参数的策略变化对比
4. **可视化**：调度甘特图、SOC轨迹、充放电策略、Pareto前沿、场景对比雷达图
5. **关键发现**：联合优化相比分层优化的增量价值、不同场景下的策略鲁棒性
